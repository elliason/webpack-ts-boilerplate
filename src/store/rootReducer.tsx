import { combineReducers } from 'redux';

const rootReducer = combineReducers({
    filelist,
    filter,
    folderlist,
    folder,
    layout,
    search,
    sort,
});

export default rootReducer;
